
Describes MML's memory model.

* Heap memory, affine.
* Borrow by defaule (no sigils)
* Function arguments marked with ~ move the ref.
* Structs, values themselves (not pointers)
  - affine if they have heap types.

  

## Rules

