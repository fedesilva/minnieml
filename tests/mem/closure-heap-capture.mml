
fn makeGreeter(~name: String): Unit -> Unit =
  { println ("Hello, " ++ name) }
;

fn loop(n: Int): Unit =
  if n > 0 then
    let greet = makeGreeter ("User" ++ (int_to_str n));
    greet ();
    loop (n - 1)
  end
;

pub fn main(): Unit =
  loop 100000
;
