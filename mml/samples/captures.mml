// Closure test: lambda captures a local binding
fn apply(f: Int -> Int, x: Int): Int = f x;

fn makeAdder(a: Int): Int -> Int =
  { x -> x + a }
;

pub fn main() =
  let add5 = makeAdder 5;
  let result = apply add5 37;
  println (int_to_str result)
;
