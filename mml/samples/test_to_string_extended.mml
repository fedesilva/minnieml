let concat_results = 
  concat 
    (concat "Zero: " (concat (to_string 0) ", "))
    (concat 
      (concat "Positive: " (concat (to_string 123) ", "))
      (concat "Large: " (to_string 1234567890))
    );

fn main() = println concat_results;
