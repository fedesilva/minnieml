// Consuming parameter not-last-use demonstration (expected: compile error)
// Passing a binding to a consuming (~) param when it's still used later is rejected.
fn consume(~s: String): Unit = println s;

fn main(): Unit =
  let s = "hello" ++ " world";
  consume s;
  println s
;
