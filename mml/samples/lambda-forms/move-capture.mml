fn apply(f: Int -> Int, x: Int): Int =
  f x;
;

fn make_move_adder(seed: Int): Int -> Int =
  ~{ x: Int -> x + seed; };
;

pub fn main(): Unit =
  let add_seed = make_move_adder 10;
  println (int_to_str (apply add_seed 10));
;
